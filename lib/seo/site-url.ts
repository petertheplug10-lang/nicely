/**
 * Canonical site origin for sitemap, robots, and metadataBase.
 * Set `NEXT_PUBLIC_SITE_URL` in production (e.g. https://www.nicozyclub.com).
 */
export function getSiteUrl(): string {
  return "https://www.nicozyclub.com";
}
